<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.task.mapper.LoveMapper">
    <select id="getMyLoveList" resultType="java.lang.String">
        SELECT task_id FROM love WHERE love_user_id = #{userId}
    </select>
</mapper>